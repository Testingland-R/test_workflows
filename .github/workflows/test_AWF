name: CI
on:
  workflow_dispatch:
    branches: [ main ]

jobs:
  call-reusable-workflow:
    uses: roquevaamonde/workflows/.github/workflows/python_CI.yml@main
    with:
      python-version: '3.11'
