name: Renovate auto approve call

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  pull_request_review:
    types: [dismissed]


jobs:
  Aprover_call:
    uses: Testingland-R/workflows/.github/workflows/auto-approve-renovate.yml@ca1bff5bc2bd9bb6e33b882c8747babfeada0370 # main
    permissions:
      pull-requests: write
      contents: read
